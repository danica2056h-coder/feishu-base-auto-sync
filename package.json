{
  "name": "feishu-base-auto-sync",
  "version": "2.0.0",
  "private": true,
  "description": "Google Sheet controlled Feishu Base connector refresh console",
  "scripts": {
    "save-auth": "node save-auth.js",
    "sync": "node sync-feishu.js",
    "control:check": "node control-client.js check",
    "test": "node --test --test-isolation=none"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "playwright": "1.54.2"
  }
}
